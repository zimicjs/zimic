import { WebSocketInterceptorWorkerOptions } from '../../types/options';

type TestMatrixCase = WebSocketInterceptorWorkerOptions;

const testMatrix: TestMatrixCase[] = [{ type: 'local' }];

export default testMatrix;
