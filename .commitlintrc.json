{
  "extends": ["@commitlint/config-conventional"],
  "rules": {
    "type-enum": [
      2,
      "always",
      ["build", "chore", "docs", "feat", "fix", "perf", "refactor", "revert", "style", "test"]
    ],
    "scope-enum": [
      2,
      "always",
      [
        "root",
        "ci",
        "release",
        "zimic-test-client",
        "#zimic",
        "#release",
        "#eslint",
        "#eslint-node",
        "#lint-staged",
        "#tsconfig"
      ]
    ],
    "scope-empty": [2, "never"]
  }
}
